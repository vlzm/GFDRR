"""The operations around the models: registry, backtest, pipeline, monitoring, evaluation.

Every module here is month-shaped, because that is how Citi Bike publishes
its files. Extract a framework interface out of it when a second scenario
exists to check the shape against.
"""
