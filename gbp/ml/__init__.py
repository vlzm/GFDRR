"""The seam a demand model plugs into: the model interface, the forecast artifact, the metrics.

Everything about how a model is built — features, model families, training —
belongs to the domain. Import the submodules directly
(``from gbp.ml.artifact import load_forecast``), never through this package.
"""
